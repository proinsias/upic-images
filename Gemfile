# Install all these gems using:
# bundle install [--gemfile=GEMFILE]
source 'https://rubygems.org'

gem 'bundle-audit'
gem 'bundler' # http://bundler.io/
gem 'image_optim'
gem 'image_optim_pack'
gem 'mdl'
